# MyOptiwealth Web

Next.js App Router shell with:

- Auth pages (`/login`, `/register`)
- Workspace-first SaaS layout
- Sidebar navigation for all core modules
- Dashboard page wired to `/dashboard/homepage`
- Placeholder pages per module ready for data wiring

Set `NEXT_PUBLIC_API_BASE_URL` to the Nest API URL.
